import { StyleSheet } from 'react-native';

const commonStyles = StyleSheet.create({
    header: {
        marginTop: 10,
        fontWeight: '900',
        color: '#154c58',
    },
    rootContainer: {
        flex: 1,
        backgroundColor: '#fff',
        paddingTop: 10,
    },
});

export default commonStyles;

